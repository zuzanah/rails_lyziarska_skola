<%= form_for(skinews) do |f| %>
  <% if skinews.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skinews.errors.count, "error") %> prohibited this skinews from being saved:</h2>

      <ul>
      <% skinews.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
